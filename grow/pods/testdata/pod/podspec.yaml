grow_version: 0.1

flags:
  static_dir: /public/

domains:
- example.com
- example.net

locales:
  default:
    regions:
    - de
    - fr
    - it
    languages:
    - en
    - ja
    - pl

deployments:
  prod:
    destination: gcs
    bucket: prod.test.growsdk.org
  staging:
    destination: gcs
    bucket: staging.test.growsdk.org
