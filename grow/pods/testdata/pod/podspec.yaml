grow_version: 0.0.24

flags:
  static_dir: /public/

domains:
- example.com
- example.net

localization:
  locales:
  - de
  - fr
  - it

deployments:

  prod:
    destination: gcs
    bucket: prod.test.growsdk.org

  staging:
    destination: gcs
    bucket: staging.test.growsdk.org
