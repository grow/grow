language: python
python:
  - 2.7
#install: "pip install -r requirements.txt --use-mirrors"
before_install:
  - git submodule update --init --recursive
  - python grow/submodules/babel/setup.py import_cldr
script: python run_tests.py
