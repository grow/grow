[[source]]
url = "https://pypi.python.org/simple"
verify_ssl = true
name = "pypi"

[packages]
colored = ">=1.3.5"
protorpc = ">=0.12.0"
pyyaml = ">=3.12"

[dev-packages]
codecov = ">=2.0.15"
coverage = ">=4.5.1"
mock = ">=2.0.0"
nose = ">=1.3.7"
pylint = ">=1.8.2"
rednose = ">=1.3.0"

[requires]
python_version = "3"
